#include "QA.h"


void QA::testGame() const {
    cout << getName() << " is testing the game." << '\n';
}

void QA::writeTestCases() const {
    cout << getName() << " is writing test cases." << '\n';
}